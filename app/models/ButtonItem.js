class ButtonItem {
    constructor(id, itemMatrix, buttonId, item) {
        this.id = id
        this.itemMatrix = itemMatrix
        this.buttonId = buttonId
        this.item = item
    }
}

module.exports = ButtonItem
