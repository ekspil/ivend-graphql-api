class ButtonItem {

    constructor(id, buttonId, multiplier, type) {
        this.id = id
        this.type = type
        this.multiplier = multiplier
        this.buttonId = buttonId
    }
}

module.exports = ButtonItem
