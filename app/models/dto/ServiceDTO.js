class ServiceDTO {

    constructor({id, name, price, billingType}) {
        this.id = id
        this.name = name
        this.price = price
        this.billingType = billingType
    }
}

module.exports = ServiceDTO
