apiVersion: apps/v1
kind: Deployment
metadata:
  name: graphql
  namespace: development
  labels:
    app: graphql
spec:
  replicas: 1
  selector:
    matchLabels:
      app: graphql
  template:
    metadata:
      namespace: development
      labels:
        app: graphql
    spec:
      containers:
      - name: graphql
        image: registry.gitlab.com/pshenmic/ivend/graphql:development
        imagePullPolicy: Always
        ports:
          - containerPort: 4000
        envFrom:
          - configMapRef:
              name: graphql-config
      imagePullSecrets:
      - name: gitlabregistrycred
